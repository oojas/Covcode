import 'package:url_launcher/url_launcher.dart';
import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

// ignore: camel_case_types
class url {
  launchURLtelangana() async {
    const url =
        'https://docs.google.com/document/d/13a2OSYCBpA1z5tj7V1Gw3BeQAXV_qFe6gvo0PZrweP0/mobilebasic';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLPD1() async {
    const url = 'https://dhoondh.com/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLBeds() async {
    const url = 'https://u07.ihx.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLPD2() async {
    const url = 'https://needplasma.org/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLPD3() async {
    const url = 'https://plasmadonor.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLPD4() async {
    const url = 'https://plasmaline.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLWillingplasmadonor() async {
    const url =
        'https://docs.google.com/spreadsheets/d/1dd2r6Kqd2kf-u6KsPgyM2EXI3bmHa_QjxUw6cSOplMo/edit#gid=190289932';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLMedicine() async {
    const url =
        'https://docs.google.com/spreadsheets/u/0/d/1J-m84rr-tV47wZrUKn41jlTTID5FNcMs5MwyYHt9fLU/htmlview';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURL() async {
    const url =
        'https://www.instagram.com/stvorg/guide/jammu-kashmir-covid-19-resources/17869596071491332/?igshid=11qck5wfsa7b7';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLGujarat() async {
    const url =
        'https://www.instagram.com/stvorg/guide/gujarat-covid-19-resources/17887002398146172/?utm_source=ig_web_copy_link&utm_campaign=&utm_medium=';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLDehradun() async {
    const url = 'https://bing.com/covid/local/dehradun_uttarakhand_india';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLUP() async {
    const url =
        'https://drive.google.com/file/d/1wI3dz1bvevT1ncLahizYq1A-QbF70OZu/view';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLWestBengal() async {
    const url =
        'https://docs.google.com/spreadsheets/d/1X7tqWu036hJYexlVUAOHv64eF5Nfn71XGtlYjMxP8lw/edit?fbclid=IwAR3Bv0qY2N5QN5XO04e0voaA8opUoPSVTWkYDmbK2p2k8aaBaHrPAHOVOfk#gid=1654953344';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLCH() async {
    const url =
        'https://docs.google.com/spreadsheets/d/1DvCXeTP2rZo5_2_TIt7_Zyi6ArEBkvwMGAIoFEe3Pi0/htmlview#';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLMaharashtra() async {
    const url =
        'https://www.mumbailive.com/en/covid-19/mumbai-maharashtra-resources-and-information-lockdown-vaccines-protocols';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLKarnataka() async {
    const url =
        'https://script.google.com/macros/s/AKfycbyHGh-AisJVBuqfZ4pfSqu001AE6JetStqpLL2xg74m-Wgdd5Ogh6yGVKG6gYuqnn1D0g/exec';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLDelhi() async {
    const url = 'http://corona.delhi.gov.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLHaryana() async {
    const url = 'http://nhmharyana.gov.in/page?id=208';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLAndhra() async {
    const url = 'https://linktr.ee/vjafightscovid';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLTN() async {
    const url = 'https://stopcorona.tn.gov.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLLucknow() async {
    const url =
        'https://docs.google.com/spreadsheets/d/1roxOi2_Uw4YBzLd5s8vC8cp6lbuM9016tWeWTcx2q5Y/edit#gid=0';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLKolkata() async {
    const url = 'https://linktr.ee/covid_kolkata_directory';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLChandigarh() async {
    const url = 'http://chdcovid19.in/index.php/page/useful-resources';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLPune() async {
    const url = 'https://linktr.ee/CovidDirectoryPune';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLJharkhand() async {
    const url =
        'https://docs.google.com/spreadsheets/d/1AFQW2fHd-6b-FdqyXCsZjkYGDkj8jyj5P8E79KjJQwk/htmlview';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLKerala() async {
    const url =
        'https://covid19jagratha.kerala.nic.in/home/addHospitalDashBoard';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLRajasthan() async {
    const url =
        'https://covidinfo.rajasthan.gov.in/covid19bedsstatusjaipur.aspx';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLMumbai() async {
    const url = 'https://linktr.ee/covid_directory_mumbai';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLOdhisha() async {
    const url = 'https://health.odisha.gov.in/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLBangalore() async {
    const url =
        'https://www.thenewsminute.com/article/bengaluru-covid-19-labs-beds-ambulances-list-numbers-you-may-need-147443';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }

  launchURLMP() async {
    const url = 'https://covidmp.com/';
    if (await canLaunch(url)) {
      await launch(url);
    } else {
      throw 'Could not launch $url';
    }
  }
}
